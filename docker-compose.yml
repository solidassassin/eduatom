networks:
  proxy:
    external: true

services:
  db:
    image: postgres
    env_file:
      - db.env
    volumes:
      - /var/lib/postgresql/data:/var/lib/postgresql/data
    restart: always
  nextjs:
    build: ./
    env_file:
      - next.env
    networks:
      - proxy
